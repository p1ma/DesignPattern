@NAME = Template method
@UML = template.png
@RESUME_FROM = https://en.wikipedia.org/wiki/Template_method_pattern
@DESCRIPTION = Define the skeleton of an algorithm in an operation, deferring some steps to subclasses. Lets subclasses redefine certain steps of an algorithm without changing the
algorithm's structure.

